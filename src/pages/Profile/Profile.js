import ProfileContainer from "../../components/ProfileContainer/ProfileContainer";

const Profile = () => {
  return (
    <div>
      <ProfileContainer />
    </div>
  );
};

export default Profile;
